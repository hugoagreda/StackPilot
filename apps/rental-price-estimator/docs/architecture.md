# MVP Architecture

- Frontend React (form + results + simple report)
- Backend FastAPI (estimation endpoint + health)
- PostgreSQL (properties, estimates, market data)
- Batch ingestion pipeline (future phase)
